# Explicit local demo mode. Never use demo credentials for a public deployment.
DEMO_MODE=true
# For MySQL mode: set DEMO_MODE=false and configure all values below.
# DATABASE_URL=mysql://user:password@localhost:3306/bytefx
# SESSION_SECRET=replace-with-at-least-32-random-characters
# ACCOUNTANT_EMAIL=accountant@bytefx.com
# ACCOUNTANT_PASSWORD_HASH=scrypt-salt:hash (see README)

# APP_URL=https://your-internal-domain.example
